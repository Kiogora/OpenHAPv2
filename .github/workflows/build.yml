on: push
jobs:
  build_firmware:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository code
      uses: actions/checkout@v2
    - name: List files
      run:
        ls
    - name: Change directory
      run:
        cd firmware
    - name: Build firmware
      run:
        docker run --rm -v $PWD:/usr/src/app -w /usr/src/app espressif/idf:release-v4.3 build